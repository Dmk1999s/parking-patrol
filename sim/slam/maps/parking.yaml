image: parking.pgm
mode: trinary
resolution: 0.05
origin: [-0.4, -0.493, 0]
negate: 0
occupied_thresh: 0.65
# 🚨 0.196 이어야 한다 — map_saver_cli 기본값(0.25)으로 저장하면 미탐사
#    픽셀(205, p=0.196)이 "빈 공간"으로 발행돼 Nav2 가 차량 뒤 가림 영역을
#    통과 가능으로 보고 차 사이 틈으로 파고든다 (로봇 끼임 실측 2회의 뿌리).
#    지도를 다시 저장하면 이 값도 다시 0.196 으로 고칠 것.
free_thresh: 0.196
